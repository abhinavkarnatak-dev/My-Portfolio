import React from 'react'
import SkillsCard from './SkillsCard'

const Skills = () => {
    return (
        <section id="skills" className="min-h-screen flex justify-center bg-black">
            <div className="mt-10">
                <SkillsCard/>
            </div>
        </section>
    );
};

export default Skills;